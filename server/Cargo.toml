[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum.workspace = true
tokio.workspace = true
surrealdb.workspace = true
jiff.workspace = true
axum-rate-limiter = { path = "../lib", features = ["surrealdb"] }
